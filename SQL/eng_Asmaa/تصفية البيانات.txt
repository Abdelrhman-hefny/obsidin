
---

## 📌 شروط المقارنة في SQL Server

شروط المقارنة بتستخدم داخل جملة `WHERE` عشان تصفي البيانات حسب شروط معينة.

---

### ✅ `=` (يساوي)

يستخدم للمقارنة بين عمود وقيمة معينة.

```sql
SELECT * FROM students WHERE grade = 'A';
```

> يعرض الطلاب اللي درجتهم A فقط.

---

### ✅ `>` (أكبر من)

```sql
SELECT * FROM students WHERE age > 20;
```

> يعرض الطلاب اللي أعمارهم أكبر من 20.

---

### ✅ `<` (أقل من)

```sql
SELECT * FROM students WHERE age < 18;
```

> يعرض الطلاب اللي أعمارهم أقل من 18.

---

### ✅ `>=` و `<=` (أكبر من أو يساوي / أقل من أو يساوي)

```sql
SELECT * FROM students WHERE age >= 18;
SELECT * FROM students WHERE age <= 25;
```

---

### ✅ `BETWEEN` (بين قيمتين)

يستخدم لتحديد مدى معين من القيم.

```sql
SELECT * FROM students WHERE age BETWEEN 18 AND 25;
```

> يعرض الطلاب اللي أعمارهم من 18 إلى 25 **شاملًا القيمتين**.

---

### ✅ `IN` (ضمن قائمة قيم)

يستخدم لما تكون عايز تقارن مع **أكثر من قيمة**.

```sql
SELECT * FROM students WHERE grade IN ('A', 'B');
```

> يعرض الطلاب اللي درجتهم A أو B.

---

### ✅ `LIKE` (مطابقة جزئية)

يُستخدم للبحث عن **نمط (pattern)** في النصوص.

| الرمز | المعنى                              |
| ----- | ----------------------------------- |
| `%`   | أي عدد من الحروف (بما فيهم ولا حرف) |
| `_`   | حرف واحد فقط                        |

```sql
-- اسم يبدأ بحرف A:
SELECT * FROM students WHERE name LIKE 'A%';

-- اسم يحتوي على ah:
SELECT * FROM students WHERE name LIKE '%ah%';

-- اسم ينتهي بحرف d:
SELECT * FROM students WHERE name LIKE '%d';
```

---

### ✅ `IS NULL` (القيمة فارغة)

يُستخدم لفحص ما إذا كانت القيمة **NULL** (يعني غير موجودة أو لم تُحدد).

```sql
SELECT * FROM students WHERE email IS NULL;
```

> يعرض الطلاب اللي ما كتبوش بريد إلكتروني.

---

### ✅ `IS NOT NULL` (القيمة ليست فارغة)

```sql
SELECT * FROM students WHERE email IS NOT NULL;
```

---

